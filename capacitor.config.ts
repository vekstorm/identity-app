import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vekstorm.identity',
  appName: 'identity-app',
  webDir: 'dist/identity-app/browser'
};

export default config;
